---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: redis-postiz
spec:
  interval: 5m
  releaseName: redis-postiz
  chartRef:
    kind: OCIRepository
    name: redis
    namespace: flux-system
  values:
    auth:
      enabled: true
      existingSecret: redis-postiz-secrets
      existingSecretPasswordKey: REDIS_PASSWORD
    master:
      persistence:
        existingClaim: redis-postiz-data
