---
# This file should be encrypted with SOPS before committing
# Example command: sops -e -i pangolin-secrets.sops.yaml
#
# To configure:
# 1. Choose a domain for Pangolin (e.g., vpn.yourdomain.com)
# 2. Set a strong admin password
# 3. Encrypt with SOPS: sops -e -i pangolin-secrets.sops.yaml
# 4. Commit the encrypted file
#
# After deployment:
# - Access Pangolin at https://vpn.yourdomain.com (via CF Tunnel)
# - Login with admin credentials
# - Create resources and clients
#
# apiVersion: v1
# kind: Secret
# metadata:
#   name: pangolin-secrets
#   namespace: network
# type: Opaque
# stringData:
#   PANGOLIN_DOMAIN: "vpn.yourdomain.com"
#   PANGOLIN_ADMIN_EMAIL: "admin@yourdomain.com"
#   PANGOLIN_ADMIN_PASSWORD: "your-strong-password-here"

