---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pics-ingress
  annotations:
    kubernetes.io/ingress.class: traefik
    cert-manager.io/cluster-issuer: "letsencrypt-production"
spec:
  tls:
    - hosts:
        - "pics.${PUBLIC_HOSTNAME}"
      secretName: pics-tls
  rules:
    - host: "pics.${PUBLIC_HOSTNAME}"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ak-outpost-media
                port:
                  number: 9000 
