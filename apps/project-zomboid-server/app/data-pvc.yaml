---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: project-zomboid-server-data-pvc
spec:
  storageClassName: longhorn-xfs
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
