---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: photos-ingress
  annotations:
    kubernetes.io/ingress.class: traefik
    cert-manager.io/cluster-issuer: "letsencrypt-production"
spec:
  tls:
    - hosts:
        - "photos.${PUBLIC_HOSTNAME}"
      secretName: photos-tls
  rules:
    - host: "photos.${PUBLIC_HOSTNAME}"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ak-outpost-media
                port:
                  number: 9000 
