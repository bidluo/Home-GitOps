---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: uptime-kuma
spec:
  interval: 5m
  chart:
    spec:
      chart: app-template
      version: 3.0.4
      sourceRef:
        kind: HelmRepository
        name: bjw-s-charts
        namespace: flux-system
  values:
    controllers:
      main:
        containers:
          main:
            image:
              repository: louislam/uptime-kuma
              tag: 1.23.11-alpine
    service:
      main:
        controller: main
        ports:
          http:
            enabled: true
            port: 3001
    persistence:
      main-config:
        existingClaim: uptime-kuma-config
        globalMounts:
          - path: /app/data
    ingress:
      main:
        enabled: true
        hosts:
          - host: "uptime.${PUBLIC_HOSTNAME}"
            paths:
              - path: /
                pathType: Prefix
                service:
                  name: ak-outpost-monitoring 
                  port: 9000