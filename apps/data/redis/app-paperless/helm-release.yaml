---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: redis-paperless
spec:
  interval: 5m
  releaseName: redis-paperless
  chartRef:
    kind: OCIRepository
    name: redis
    namespace: flux-system
  values:
    auth:
      enabled: true
      existingSecret: redis-paperless-secrets
      existingSecretPasswordKey: REDIS_PASSWORD
    master:
      persistence:
        existingClaim: redis-paperless-data
