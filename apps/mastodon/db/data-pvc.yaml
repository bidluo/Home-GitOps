---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mastodon-postgresql-data
spec:
  storageClassName: longhorn-backup
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
