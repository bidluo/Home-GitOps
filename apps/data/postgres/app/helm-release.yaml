---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: postgres 
spec:
  interval: 5m
  chart:
    spec:
      chart: cloudnative-pg
      version: 0.18.1
      sourceRef:
        kind: HelmRepository
        name: cnpg-charts
        namespace: flux-system