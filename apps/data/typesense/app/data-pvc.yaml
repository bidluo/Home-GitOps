apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: typesense-data 
  namespace: data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: longhorn-backup
