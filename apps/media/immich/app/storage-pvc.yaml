apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-storage
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
  storageClassName: longhorn-backup
