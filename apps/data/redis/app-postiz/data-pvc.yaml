---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-postiz-data
  namespace: data
spec:
  storageClassName: longhorn-backup
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
