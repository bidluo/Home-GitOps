---
apiVersion: v1
kind: Secret
metadata:
  name: postgres-bitnami-secrets
  namespace: data
stringData:
  postgresql-password: "${SECRET_N8N_POSTGRES_PASS}"
  # This would typically be encrypted by SOPS before being committed 